
# Claude Code local config (may accumulate secrets in permissions allow-list)
.claude/settings.local.json

# local junk / exports (not part of the site)
nul
extglob.FullName
/Google/
/ASCE 7-22/
LOGO-FIX-INSTRUCTIONS.md

.vercel
.env*.local
